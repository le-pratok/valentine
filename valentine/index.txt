<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Pour Anaïs</title>

<style>
body {
    margin: 0;
    min-height: 100vh;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: Arial, sans-serif;
    overflow: hidden;
    cursor: none;
}

.box {
    background: white;
    padding: 20px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 0 20px rgba(0,0,0,0.2);
    width: 90%;
    max-width: 380px;
    z-index: 10;
    position: relative;
    overflow-y: auto;
    word-wrap: break-word;
}

h1 {
    color: #ff4d6d;
    font-size: 22px;
    margin-bottom: 10px;
}

p {
    margin: 5px 0;
}

.small-text {
    font-size: 16px; /* texte plus petit pour la 2ème phrase */
}

img {
    width: 200px;
    border-radius: 10px;
    margin: 10px 0;
}

.buttons {
    margin-top: 15px;
    position: relative;
    height: 100px;
}

button {
    padding: 10px 20px;
    font-size: 18px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    position: absolute;
}

#yes {
    background-color: #ff4d6d;
    color: white;
    left: 25%;
}

#no {
    background-color: #ccc;
    left: 55%;
}

#result {
    margin-top: 10px;
    font-size: 18px;
    color: #ff4d6d;
    font-weight: bold;
}

#patate {
    width: 220px;
    animation: spin 2s linear infinite;
    margin-top: 10px;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

.heart, .confetti {
    position: fixed;
    pointer-events: none;
    z-index: 5;
    animation: floatUp 5s linear forwards;
}

@keyframes floatUp {
    from { transform: translateY(0); opacity: 1; }
    to { transform: translateY(-100vh); opacity: 0; }
}

#mouseHeart {
    position: fixed;
    pointer-events: none;
    font-size: 25px;
    z-index: 100;
    transform: translate(-50%, -50%);
}
</style>
</head>

<body>

<div id="mouseHeart">❤️</div>

<div class="box">
    <h1 id="title">Anaïs, veux-tu être ma Valentine ?</h1>
    <img id="mainImage" src="ours.jpg" alt="ours">

    <div class="buttons">
        <button id="yes">Oui ❤️</button>
        <button id="no">Non 😅</button>
    </div>

    <div id="result"></div>
</div>

<script>
const noBtn = document.getElementById("no");
const yesBtn = document.getElementById("yes");
const result = document.getElementById("result");
const title = document.getElementById("title");
const mouseHeart = document.getElementById("mouseHeart");
const mainImage = document.getElementById("mainImage");

/* bouton NON qui fuit */
noBtn.addEventListener("mouseover", () => {
    const x = Math.random() * 250 - 125;
    const y = Math.random() * 150 - 75;
    noBtn.style.transform = `translate(${x}px, ${y}px)`;
});

/* bouton OUI */
yesBtn.addEventListener("click", () => {

    // première ligne du titre
    title.textContent = "J'étais sûr que tu dirais OUI ❤️";

    // cacher l'ours
    mainImage.style.display = "none";

    // afficher la photo, texte et patate avec deuxième phrase plus petite
    result.innerHTML = `
        <p class="small-text">De toute façon t'avais pas le choix 😏</p>
        <img src="tonimage.jpeg" alt="Ma photo" style="width:200px; border-radius:10px; margin-bottom:10px;">
        <p>Je t'aime sale gosse ❤️</p>
        <img id="patate" src="patate.png" alt="patate">
    `;

    yesBtn.style.display = "none";
    noBtn.style.display = "none";

    startHearts();
    startConfetti();
});

/* pluie de cœurs */
function startHearts() {
    setInterval(() => {
        const heart = document.createElement("div");
        heart.className = "heart";
        heart.textContent = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.fontSize = Math.random() * 20 + 15 + "px";
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 5000);
    }, 300);
}

/* pluie de confettis */
function startConfetti() {
    const colors = ["#f94144","#f3722c","#f9c74f","#90be6d","#577590"];

    setInterval(() => {
        const confetti = document.createElement("div");
        confetti.className = "confetti";
        confetti.textContent = "🎉";
        confetti.style.left = Math.random() * 100 + "vw";
        confetti.style.fontSize = Math.random() * 25 + 15 + "px";
        confetti.style.color = colors[Math.floor(Math.random()*colors.length)];
        document.body.appendChild(confetti);

        setTimeout(() => confetti.remove(), 5000);
    }, 500);
}

/* cœur qui suit la souris */
document.addEventListener("mousemove", (e) => {
    mouseHeart.style.left = e.clientX + "px";
    mouseHeart.style.top = e.clientY + "px";
});
</script>

</body>
</html>
